/**
 * 应用
 */

var Object = require('Object');

var App = Class.inherit('App',Object);

App.prototype.addPlugin = function(plugin) {
	
};

App.prototype.addService = function(service) {
	
};

App.prototype.addTaskType = function(taskType) {
	
};

module.exports = Service;
